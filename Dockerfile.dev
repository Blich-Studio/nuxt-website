# Development Dockerfile with hot reload
FROM oven/bun:1-slim

WORKDIR /app

# Expose port
EXPOSE 3000

# Install deps and start dev server at runtime (source is volume-mounted)
CMD ["sh", "-c", "bun install && bun run dev"]
