# Nuxt Website - Cloud Build Pipeline
# Builds Docker image, pushes to Artifact Registry, deploys via Terraform

steps:
  # Step 1: Build Docker image
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-image'
    args:
      - 'build'
      - '-t'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_ARTIFACT_REPO}/${_SERVICE_NAME}:${SHORT_SHA}'
      - '-t'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_ARTIFACT_REPO}/${_SERVICE_NAME}:latest'
      - '.'

  # Step 2: Push image
  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-image'
    args: ['push', '--all-tags', '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_ARTIFACT_REPO}/${_SERVICE_NAME}']

  # Step 3: Download shared Terraform modules from GCS
  - name: 'gcr.io/cloud-builders/gsutil'
    id: 'fetch-tf-modules'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        mkdir -p terraform-modules && \
        gsutil -m cp -r gs://blichstudio-infras-terraform-state/terraform-modules/modules terraform-modules/

  # Step 4: Terraform init
  - name: 'hashicorp/terraform:1.9'
    id: 'tf-init'
    dir: 'deploy'
    args: ['init', '-input=false']

  # Step 5: Terraform plan
  - name: 'hashicorp/terraform:1.9'
    id: 'tf-plan'
    dir: 'deploy'
    args:
      - 'plan'
      - '-input=false'
      - '-var=container_image=${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_ARTIFACT_REPO}/${_SERVICE_NAME}:${SHORT_SHA}'
      - '-out=tfplan'

  # Step 6: Terraform apply
  - name: 'hashicorp/terraform:1.9'
    id: 'tf-apply'
    dir: 'deploy'
    args: ['apply', '-input=false', 'tfplan']

substitutions:
  _REGION: 'europe-west1'
  _SERVICE_NAME: 'blich-website'
  _ARTIFACT_REPO: 'blich-studio'

options:
  machineType: 'E2_HIGHCPU_8'
  logging: 'CLOUD_LOGGING_ONLY'

timeout: '600s'
